{
  "name": "@chrono/domain",
  "version": "0.1.0",
  "description": "CHRONO Framework — pure domain state machines and invariants (no I/O)",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
